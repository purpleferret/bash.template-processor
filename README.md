# bash.template-processor
The processor processes template files of json.  
You can specify a template with contained expressions to be processed by bash.  
The processor can be executed from a bash like
```bash
./json.processor.sh
```
